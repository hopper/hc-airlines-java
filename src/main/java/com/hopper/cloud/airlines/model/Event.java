/*
 * Airline API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.hopper.cloud.airlines.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.hopper.cloud.airlines.model.BookingConfirmed;
import com.hopper.cloud.airlines.model.OffersDisplayed;
import java.io.IOException;
import java.time.OffsetDateTime;
import java.util.Arrays;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.hopper.cloud.airlines.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2025-04-02T18:35:20.974206513Z[Etc/UTC]", comments = "Generator version: 7.10.0")
public class Event extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(Event.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!Event.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'Event' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<BookingConfirmed> adapterBookingConfirmed = gson.getDelegateAdapter(this, TypeToken.get(BookingConfirmed.class));
            final TypeAdapter<OffersDisplayed> adapterOffersDisplayed = gson.getDelegateAdapter(this, TypeToken.get(OffersDisplayed.class));

            return (TypeAdapter<T>) new TypeAdapter<Event>() {
                @Override
                public void write(JsonWriter out, Event value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `BookingConfirmed`
                    if (value.getActualInstance() instanceof BookingConfirmed) {
                        JsonElement element = adapterBookingConfirmed.toJsonTree((BookingConfirmed)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `OffersDisplayed`
                    if (value.getActualInstance() instanceof OffersDisplayed) {
                        JsonElement element = adapterOffersDisplayed.toJsonTree((OffersDisplayed)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: BookingConfirmed, OffersDisplayed");
                }

                @Override
                public Event read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize BookingConfirmed
                    try {
                        // validate the JSON object to see if any exception is thrown
                        BookingConfirmed.validateJsonElement(jsonElement);
                        actualAdapter = adapterBookingConfirmed;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'BookingConfirmed'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for BookingConfirmed failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'BookingConfirmed'", e);
                    }
                    // deserialize OffersDisplayed
                    try {
                        // validate the JSON object to see if any exception is thrown
                        OffersDisplayed.validateJsonElement(jsonElement);
                        actualAdapter = adapterOffersDisplayed;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'OffersDisplayed'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for OffersDisplayed failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'OffersDisplayed'", e);
                    }

                    if (match == 1) {
                        Event ret = new Event();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for Event: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public Event() {
        super("oneOf", Boolean.FALSE);
    }

    public Event(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("BookingConfirmed", BookingConfirmed.class);
        schemas.put("OffersDisplayed", OffersDisplayed.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return Event.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * BookingConfirmed, OffersDisplayed
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof BookingConfirmed) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof OffersDisplayed) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be BookingConfirmed, OffersDisplayed");
    }

    /**
     * Get the actual instance, which can be the following:
     * BookingConfirmed, OffersDisplayed
     *
     * @return The actual instance (BookingConfirmed, OffersDisplayed)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `BookingConfirmed`. If the actual instance is not `BookingConfirmed`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `BookingConfirmed`
     * @throws ClassCastException if the instance is not `BookingConfirmed`
     */
    public BookingConfirmed getBookingConfirmed() throws ClassCastException {
        return (BookingConfirmed)super.getActualInstance();
    }

    /**
     * Get the actual instance of `OffersDisplayed`. If the actual instance is not `OffersDisplayed`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `OffersDisplayed`
     * @throws ClassCastException if the instance is not `OffersDisplayed`
     */
    public OffersDisplayed getOffersDisplayed() throws ClassCastException {
        return (OffersDisplayed)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to Event
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with BookingConfirmed
        try {
            BookingConfirmed.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for BookingConfirmed failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with OffersDisplayed
        try {
            OffersDisplayed.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for OffersDisplayed failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for Event with oneOf schemas: BookingConfirmed, OffersDisplayed. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of Event given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of Event
     * @throws IOException if the JSON string is invalid with respect to Event
     */
    public static Event fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, Event.class);
    }

    /**
     * Convert an instance of Event to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

