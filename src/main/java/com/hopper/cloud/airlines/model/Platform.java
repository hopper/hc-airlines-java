/*
 * Airline API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.hopper.cloud.airlines.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.hopper.cloud.airlines.model.App;
import com.hopper.cloud.airlines.model.Browser;
import com.hopper.cloud.airlines.model.OperatingSystem;
import com.hopper.cloud.airlines.model.Web;
import java.io.IOException;
import java.util.Arrays;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.hopper.cloud.airlines.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2025-04-02T18:35:20.974206513Z[Etc/UTC]", comments = "Generator version: 7.10.0")
public class Platform extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(Platform.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!Platform.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'Platform' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<App> adapterApp = gson.getDelegateAdapter(this, TypeToken.get(App.class));
            final TypeAdapter<Web> adapterWeb = gson.getDelegateAdapter(this, TypeToken.get(Web.class));

            return (TypeAdapter<T>) new TypeAdapter<Platform>() {
                @Override
                public void write(JsonWriter out, Platform value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `App`
                    if (value.getActualInstance() instanceof App) {
                        JsonElement element = adapterApp.toJsonTree((App)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Web`
                    if (value.getActualInstance() instanceof Web) {
                        JsonElement element = adapterWeb.toJsonTree((Web)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: App, Web");
                }

                @Override
                public Platform read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize App
                    try {
                        // validate the JSON object to see if any exception is thrown
                        App.validateJsonElement(jsonElement);
                        actualAdapter = adapterApp;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'App'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for App failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'App'", e);
                    }
                    // deserialize Web
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Web.validateJsonElement(jsonElement);
                        actualAdapter = adapterWeb;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Web'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Web failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Web'", e);
                    }

                    if (match == 1) {
                        Platform ret = new Platform();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for Platform: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public Platform() {
        super("oneOf", Boolean.FALSE);
    }

    public Platform(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("App", App.class);
        schemas.put("Web", Web.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return Platform.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * App, Web
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof App) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Web) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be App, Web");
    }

    /**
     * Get the actual instance, which can be the following:
     * App, Web
     *
     * @return The actual instance (App, Web)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `App`. If the actual instance is not `App`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `App`
     * @throws ClassCastException if the instance is not `App`
     */
    public App getApp() throws ClassCastException {
        return (App)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Web`. If the actual instance is not `Web`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Web`
     * @throws ClassCastException if the instance is not `Web`
     */
    public Web getWeb() throws ClassCastException {
        return (Web)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to Platform
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with App
        try {
            App.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for App failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with Web
        try {
            Web.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Web failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for Platform with oneOf schemas: App, Web. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of Platform given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of Platform
     * @throws IOException if the JSON string is invalid with respect to Platform
     */
    public static Platform fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, Platform.class);
    }

    /**
     * Convert an instance of Platform to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

