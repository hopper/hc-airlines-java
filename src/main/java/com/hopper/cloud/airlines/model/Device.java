/*
 * Airline API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.hopper.cloud.airlines.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.hopper.cloud.airlines.model.Desktop;
import com.hopper.cloud.airlines.model.Mobile;
import com.hopper.cloud.airlines.model.Platform;
import com.hopper.cloud.airlines.model.Tablet;
import java.io.IOException;
import java.util.Arrays;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.hopper.cloud.airlines.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.10.0")
public class Device extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(Device.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!Device.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'Device' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<Desktop> adapterDesktop = gson.getDelegateAdapter(this, TypeToken.get(Desktop.class));
            final TypeAdapter<Mobile> adapterMobile = gson.getDelegateAdapter(this, TypeToken.get(Mobile.class));
            final TypeAdapter<Tablet> adapterTablet = gson.getDelegateAdapter(this, TypeToken.get(Tablet.class));

            return (TypeAdapter<T>) new TypeAdapter<Device>() {
                @Override
                public void write(JsonWriter out, Device value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `Desktop`
                    if (value.getActualInstance() instanceof Desktop) {
                        JsonElement element = adapterDesktop.toJsonTree((Desktop)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Mobile`
                    if (value.getActualInstance() instanceof Mobile) {
                        JsonElement element = adapterMobile.toJsonTree((Mobile)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Tablet`
                    if (value.getActualInstance() instanceof Tablet) {
                        JsonElement element = adapterTablet.toJsonTree((Tablet)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: Desktop, Mobile, Tablet");
                }

                @Override
                public Device read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize Desktop
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Desktop.validateJsonElement(jsonElement);
                        actualAdapter = adapterDesktop;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Desktop'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Desktop failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Desktop'", e);
                    }
                    // deserialize Mobile
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Mobile.validateJsonElement(jsonElement);
                        actualAdapter = adapterMobile;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Mobile'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Mobile failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Mobile'", e);
                    }
                    // deserialize Tablet
                    try {
                        // validate the JSON object to see if any exception is thrown
                        Tablet.validateJsonElement(jsonElement);
                        actualAdapter = adapterTablet;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Tablet'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Tablet failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Tablet'", e);
                    }

                    if (match == 1) {
                        Device ret = new Device();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for Device: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public Device() {
        super("oneOf", Boolean.FALSE);
    }

    public Device(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("Desktop", Desktop.class);
        schemas.put("Mobile", Mobile.class);
        schemas.put("Tablet", Tablet.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return Device.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * Desktop, Mobile, Tablet
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof Desktop) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Mobile) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Tablet) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be Desktop, Mobile, Tablet");
    }

    /**
     * Get the actual instance, which can be the following:
     * Desktop, Mobile, Tablet
     *
     * @return The actual instance (Desktop, Mobile, Tablet)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `Desktop`. If the actual instance is not `Desktop`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Desktop`
     * @throws ClassCastException if the instance is not `Desktop`
     */
    public Desktop getDesktop() throws ClassCastException {
        return (Desktop)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Mobile`. If the actual instance is not `Mobile`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Mobile`
     * @throws ClassCastException if the instance is not `Mobile`
     */
    public Mobile getMobile() throws ClassCastException {
        return (Mobile)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Tablet`. If the actual instance is not `Tablet`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Tablet`
     * @throws ClassCastException if the instance is not `Tablet`
     */
    public Tablet getTablet() throws ClassCastException {
        return (Tablet)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to Device
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with Desktop
        try {
            Desktop.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Desktop failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with Mobile
        try {
            Mobile.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Mobile failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with Tablet
        try {
            Tablet.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Tablet failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for Device with oneOf schemas: Desktop, Mobile, Tablet. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of Device given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of Device
     * @throws IOException if the JSON string is invalid with respect to Device
     */
    public static Device fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, Device.class);
    }

    /**
     * Convert an instance of Device to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

