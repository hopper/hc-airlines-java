/*
 * Airline API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1.1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.hopper.cloud.airlines.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.hopper.cloud.airlines.model.AgentReseller;
import com.hopper.cloud.airlines.model.Api;
import com.hopper.cloud.airlines.model.Direct;
import com.hopper.cloud.airlines.model.OnlineBookingTool;
import com.hopper.cloud.airlines.model.WhiteLabel;
import java.io.IOException;
import java.util.Arrays;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.hopper.cloud.airlines.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-06-08T20:04:46.237393626Z[Etc/UTC]", comments = "Generator version: 7.10.0")
public class Channel extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(Channel.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!Channel.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'Channel' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<AgentReseller> adapterAgentReseller = gson.getDelegateAdapter(this, TypeToken.get(AgentReseller.class));
            final TypeAdapter<Api> adapterApi = gson.getDelegateAdapter(this, TypeToken.get(Api.class));
            final TypeAdapter<Direct> adapterDirect = gson.getDelegateAdapter(this, TypeToken.get(Direct.class));
            final TypeAdapter<OnlineBookingTool> adapterOnlineBookingTool = gson.getDelegateAdapter(this, TypeToken.get(OnlineBookingTool.class));
            final TypeAdapter<WhiteLabel> adapterWhiteLabel = gson.getDelegateAdapter(this, TypeToken.get(WhiteLabel.class));

            return (TypeAdapter<T>) new TypeAdapter<Channel>() {
                @Override
                public void write(JsonWriter out, Channel value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `AgentReseller`
                    if (value.getActualInstance() instanceof AgentReseller) {
                        JsonElement element = adapterAgentReseller.toJsonTree((AgentReseller)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Api`
                    if (value.getActualInstance() instanceof Api) {
                        JsonElement element = adapterApi.toJsonTree((Api)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Direct`
                    if (value.getActualInstance() instanceof Direct) {
                        JsonElement element = adapterDirect.toJsonTree((Direct)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `OnlineBookingTool`
                    if (value.getActualInstance() instanceof OnlineBookingTool) {
                        JsonElement element = adapterOnlineBookingTool.toJsonTree((OnlineBookingTool)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `WhiteLabel`
                    if (value.getActualInstance() instanceof WhiteLabel) {
                        JsonElement element = adapterWhiteLabel.toJsonTree((WhiteLabel)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: AgentReseller, Api, Direct, OnlineBookingTool, WhiteLabel");
                }

                @Override
                public Channel read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize AgentReseller
                    try {
                        actualAdapter = adapterAgentReseller;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'AgentReseller'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for AgentReseller failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'AgentReseller'", e);
                    }
                    // deserialize Api
                    try {
                        actualAdapter = adapterApi;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Api'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Api failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Api'", e);
                    }
                    // deserialize Direct
                    try {
                        actualAdapter = adapterDirect;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Direct'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Direct failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Direct'", e);
                    }
                    // deserialize OnlineBookingTool
                    try {
                        actualAdapter = adapterOnlineBookingTool;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'OnlineBookingTool'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for OnlineBookingTool failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'OnlineBookingTool'", e);
                    }
                    // deserialize WhiteLabel
                    try {
                        actualAdapter = adapterWhiteLabel;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'WhiteLabel'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for WhiteLabel failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'WhiteLabel'", e);
                    }

                    if (match == 1) {
                        Channel ret = new Channel();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for Channel: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public Channel() {
        super("oneOf", Boolean.FALSE);
    }

    public Channel(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("AgentReseller", AgentReseller.class);
        schemas.put("Api", Api.class);
        schemas.put("Direct", Direct.class);
        schemas.put("OnlineBookingTool", OnlineBookingTool.class);
        schemas.put("WhiteLabel", WhiteLabel.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return Channel.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * AgentReseller, Api, Direct, OnlineBookingTool, WhiteLabel
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof AgentReseller) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Api) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Direct) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof OnlineBookingTool) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof WhiteLabel) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be AgentReseller, Api, Direct, OnlineBookingTool, WhiteLabel");
    }

    /**
     * Get the actual instance, which can be the following:
     * AgentReseller, Api, Direct, OnlineBookingTool, WhiteLabel
     *
     * @return The actual instance (AgentReseller, Api, Direct, OnlineBookingTool, WhiteLabel)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `AgentReseller`. If the actual instance is not `AgentReseller`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `AgentReseller`
     * @throws ClassCastException if the instance is not `AgentReseller`
     */
    public AgentReseller getAgentReseller() throws ClassCastException {
        return (AgentReseller)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Api`. If the actual instance is not `Api`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Api`
     * @throws ClassCastException if the instance is not `Api`
     */
    public Api getApi() throws ClassCastException {
        return (Api)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Direct`. If the actual instance is not `Direct`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Direct`
     * @throws ClassCastException if the instance is not `Direct`
     */
    public Direct getDirect() throws ClassCastException {
        return (Direct)super.getActualInstance();
    }

    /**
     * Get the actual instance of `OnlineBookingTool`. If the actual instance is not `OnlineBookingTool`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `OnlineBookingTool`
     * @throws ClassCastException if the instance is not `OnlineBookingTool`
     */
    public OnlineBookingTool getOnlineBookingTool() throws ClassCastException {
        return (OnlineBookingTool)super.getActualInstance();
    }

    /**
     * Get the actual instance of `WhiteLabel`. If the actual instance is not `WhiteLabel`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `WhiteLabel`
     * @throws ClassCastException if the instance is not `WhiteLabel`
     */
    public WhiteLabel getWhiteLabel() throws ClassCastException {
        return (WhiteLabel)super.getActualInstance();
    }

    /**
     * Create an instance of Channel given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of Channel
     * @throws IOException if the JSON string is invalid with respect to Channel
     */
    public static Channel fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, Channel.class);
    }

    /**
     * Convert an instance of Channel to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

